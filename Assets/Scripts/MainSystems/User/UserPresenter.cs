using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UserPresenter : MonoBehaviour
{
    [SerializeField] public UserView userView;
    [SerializeField] public UserModel userModel;

    [SerializeField] public VediceManager vediceManager;

    // Start is called before the first frame update
    void Start()
    {
        
    }
}
